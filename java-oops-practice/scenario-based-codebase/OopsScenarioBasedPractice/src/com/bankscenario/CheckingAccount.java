package com.scenariobasedquestion.bankscenario;

// CheckingAccount class extends BankAccount
public class CheckingAccount extends BankAccount {
    // constructor
    public CheckingAccount(double accountBalance) {
        super(accountBalance);
    }

    // maybe add some specific method, but for now same
}